server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/splunklite?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Ts07gk3867@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=always
spring.sql.init.encoding=UTF-8

app.alert-interval-seconds=60


# disable Flyway (we're applying schema fix at startup instead)
spring.flyway.enabled=false

# JWT secret for signing tokens (in production keep this secret safe)
app.jwt.secret=change-me-replace-with-secure-random-value

